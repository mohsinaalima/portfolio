export function About() {
  return (
    <section id="about" className="section-y container-page">
      {/* TODO (chunk 6): narrow editorial column, no filler adjectives. See v1 §8, §15. */}
      <p className="font-mono-label text-xs text-accent-brass">
        [ about — build pending ]
      </p>
    </section>
  );
}

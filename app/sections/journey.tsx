export function Journey() {
  return (
    <section id="journey" className="section-y container-page">
      {/* TODO (chunk 6): vertical timeline, blueprint rail. See v1 §8, v2 §2. */}
      <p className="font-mono-label text-xs text-accent-brass">
        [ journey / timeline — build pending ]
      </p>
    </section>
  );
}

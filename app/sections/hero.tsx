export function Hero() {
  return (
    <section id="top" className="section-y container-page">
      {/* TODO (chunk 2): positioning statement, availability badge, CTAs,
          ambient node-graph visualization per docs/design-strategy-v2.md §2/§8 */}
      <p className="font-mono-label text-xs text-accent-brass">
        [ hero — build pending ]
      </p>
    </section>
  );
}

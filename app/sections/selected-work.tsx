export function SelectedWork() {
  return (
    <section id="selected-work" className="section-y container-page">
      {/* TODO (chunk 3): PicScale / AI-Resume-Analyzer / kidsportal cards
          + MedLens/HealthBridge placeholder cards. See v2 §6, §7. */}
      <p className="font-mono-label text-xs text-accent-brass">
        [ selected work — build pending ]
      </p>
    </section>
  );
}

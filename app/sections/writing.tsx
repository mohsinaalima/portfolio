export function Writing() {
  return (
    <section id="writing" className="section-y container-page">
      {/* TODO (chunk 7, optional): MDX-driven notes list. See v1 §16-17. */}
      <p className="font-mono-label text-xs text-accent-brass">
        [ writing — build pending ]
      </p>
    </section>
  );
}

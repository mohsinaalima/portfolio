export function Contact() {
  return (
    <section id="contact" className="section-y container-page">
      {/* TODO (chunk 7): copy-email interaction, no form. See v1 §8, §14. */}
      <p className="font-mono-label text-xs text-accent-brass">
        [ contact — build pending ]
      </p>
    </section>
  );
}
